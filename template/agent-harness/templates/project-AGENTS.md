# <project-name>

<一句话说明产品/服务边界、技术形态和关键跨仓关系。>

## 默认工作方式

- 默认使用简体中文；代码标识符和协议字段保留英文。
- 当前事实以代码、manifest、测试、生成脚本和可运行命令为准。
- 使用 `<package-manager>`；不得混用会改写 lockfile 的其他包管理器。
- 未经明确授权，不 commit/push/发布/迁移/远端写入。

## 不可从代码推断的关键约束

- <P0/P1 规则：遗漏会造成安全、兼容、跨仓或高返工成本。>
- <跨仓 owner、消费者、迁移双写或发布顺序。>

## 按需读取

| 任务 | 事实源 / Skill |
| --- | --- |
| 开发环境与命令 | `<path>` |
| 架构或跨模块变更 | `<path>` |
| UI/Figma/设计系统 | `<path>` |
| API/数据库/部署 | `<path>` |
| 发布或迁移 | `<path>` |
| 历史输入、交接或工作状态 | `.agent-docs/core.md`，再按 `memory:` 引用读取 |

## 验证与完成

- 最小定向检查：`<command>`。
- 跨模块或生产边界变化：`<command>`。
- 行为变化同步测试与正式文档；未验证项在交付中明确列出。
- `.agent-docs` 只记录非权威记忆；稳定结论提升到 `docs/`、ADR、测试或代码。

## 演进

本文件目标不超过 120 行。能由代码、类型、lint、测试、hook 或 CI 执行的规则优先工程化；
低频详细内容迁移到 `docs/`、skill 或专题规则，并保留这里的路由入口。
